## What

Brief description of what this PR changes.

## Why

The motivation behind this change.

## Test

- [ ] `shellcheck bhkey.sh` passes locally
- [ ] `bash bhkey.sh apply` works correctly
- [ ] `bash bhkey.sh status` shows expected output
- [ ] `bash bhkey.sh reset` cleans up correctly
- [ ] Tested on macOS version:
- [ ] Tested with keyboard model:
